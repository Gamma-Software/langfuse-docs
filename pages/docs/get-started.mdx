---
description: Démarrer la génération de packages avec Aiop en quelques minutes avant de continuer dans la customization et l'utilisation de fonctions avancées.
---
import { Callout } from "nextra-theme-docs";
import { Button } from "@/components/ui/button";
import { SiPython } from "react-icons/si";
import { TbBrandVscode } from "react-icons/tb";

# Démarrage

Cette introduction rapide vous aide à commencer à utiliser Aiop rapidement. N’hesitez pas à consulter la [vidéo](/docs/demo) démonstration qui reprend les éléments de cette page.

## Prerequis

### Python

Un environnement Python 3.6+ est nécessaire pour utiliser Aiop. Si vous n'avez pas Python d’installé vous pouvez télécharger les versions suivantes, selon votre système d'exploitation:

<Tabs items={["Windows","Ubuntu","Mac OS"]}>

<Tab>
{/* Windows */}

<Cards num={3}>
  <Card icon={<SiPython size="24" />} title="Python (3.6 et +)" href="https://www.python.org/downloads/" target="_blank" />
  <Card icon={<SiPython size="24" />} title="Recommandé (3.11.8)" href="https://www.python.org/downloads/release/python-3118/" target="_blank" arrow/>
  <Card
    icon={<SiPython size="24" />}
    title="Stable et récente (3.12.2)"
    href="https://www.python.org/downloads/release/python-3122/"
    target="_blank"
  />
</Cards>

Le guide d’installation de python sur Windows peut être trouvé [ici](https://docs.python.org/3/using/windows.html#the-full-installer).

</Tab>
<Tab>
{/* Ubuntu */}

<Steps>

### Installation de Python

Installez la version recommandée de Python 3.11.8 en utilisant votre gestionnaire de paquets. Mettez à jour votre gestionnaire de paquets et installez les dépendances nécessaires. Puis ajoutez le dépôt de deadsnakes pour installer les versions de Python.

```bash copy
sudo apt-get update -y
sudo apt-get upgrade -y
sudo apt-get install build-essential libssl-dev libffi-dev python3-dev
sudo add-apt-repository ppa:deadsnakes/ppa
```

Ensuite installez la version de python de votre choix. Veuillez installer les versions supérieurs à 3.6.

```bash copy
sudo apt install python3.6
```

En revanche nous recommandons d’installer la version 3.11.8.

```bash copy
sudo apt install python3.11
```

<Callout type="info" emoji="ℹ️">
  Vous pouvez toute fois installer la dernière version 3.12. En revanche nous ne garantissons pas que Aiop fonctione.
</Callout>

Vérifiez la version de python installée avec la commande `python3 --version`.

### Installation des dépendances

Additionnellement installez des outils de développement et des bibliothèques pour Python que Aiop utilisera.

```bash copy
sudo apt-get install curl iputils-ping -y
```

### Installation de Pip
Si vous n’avez pas pip d’installez, vous pouvez l’installer avec la commande suivante:

```bash copy
sudo apt-get install python3-pip -y
```
</Steps>

### Alias

Pour simplifier l’utilisation de python, il est recommandé de créer des racourcis des commandes `python` et `pip` à `python3` et `pip3` respectivement:

```bash copy
ln -s /usr/bin/python3 /usr/bin/python
ln -s /usr/bin/pip3 /usr/bin/pip
```

</Tab>
<Tab>
{/* Mac OS */}


<Cards num={3}>
  <Card icon={<SiPython size="24" />} title="Python (3.6 et +)" href="https://www.python.org/downloads/macos/" target="_blank" />
  <Card icon={<SiPython size="24" />}title="Recommandé (3.11.8)" href="https://www.python.org/ftp/python/3.11.8/python-3.11.8-macos11.pkg" target="_blank" arrow/>
  <Card
    icon={<SiPython size="24" />}
    title="Stable et récente (3.12.2)"
    href="https://www.python.org/ftp/python/3.12.2/python-3.12.2-macos11.pkg"
    target="_blank"
  />
</Cards>

Le guide d’installation de python sur MacOS peut être trouvé [ici](https://docs.python.org/3/using/mac.html#the-full-installer).

</Tab>
</Tabs>

### Éditeur de code

Un éditeur de code est également nécessaire pour travailler avec Aiop. Vous pouvez utiliser n’importe quel éditeur de code que vous préférez. Cependant, nous recommandons d’utiliser Visual Studio Code. Nous avons spécialement conçu des racourcis pour VSCode. À l’avenir, nous developperons des [extensions](/docs/roadmap) pour VSCode.

Vous pouvez télécharger Visual Studio Code en cliquant sur le bouton ci-dessous.

<Cards>
<Card
  icon={<TbBrandVscode size="24"/>}
  title="VSCode"
  href="https://code.visualstudio.com/download"
  target="_blank"
  arrow
/>
</Cards>

## Installation

<Steps>

### Créer un compte aiop (Optionnel)

Commencez par créer un compte aiop. Si vous n’avez pas de compte, appuiyez sur le bouton ci-dessous pour en créer un.



### Installer aiop
Installer aiop est simple. Ouvrez un terminal et exécutez la commande suivante:

```bash copy
pip install aiop --index-url https://gitlab.com/api/v4/projects/52727704/packages/pypi/simple
```

Cela installera aiop et toutes ses dépendances.


### Télécharger un modèle de playbook

Pour utiliser aiop, vous avez besoin d’un [playbook](/docs/vocabulary#playbook). Vous pouvez installer un modèle démonstratif de playbook en exécutant la commande suivante:

```bash copy
aiop demo ./my-demo-playbook
```

Cela téléchargera le modèle de playbook dans le dossier `my-playbook`.

### Générer sont premier package

Vous y êtes enfin ! Vous pouvez maintenant générer votre premier package en exécutant la commande suivante:

```bash copy
cd my-playbook
aiop build archi1/model_range1/model1 --apps app1 --envs dev --contexts context1
```

<Callout type="info" emoji="✅">
  Terminé, vous venez de générer votre premier package !
</Callout>

Vous pouvez maintenant voir le package généré dans le dossier `.build` à la racine du playbook.

</Steps>



## Que faire ensuite ?

Il est maintenant temps de modifier le playbook ainsi que vous l’approprier. Passez à la partie [configuration](/docs/configuration/overview) pour apprendre à configurer votre playbook. Sinon vous pouvez suivre une vidéo d’introduction à aiop [ici](/demo).


<Cards num={2}>
  <Card href="/docs/configuration/overview" title="Configuration" image arrow>
    ![Quickstart icon (rocket)](/images/cards/rocket.svg)
  </Card>
  <Card href="/demo" title="Démo" image arrow>
    ![Quickstart icon (rocket)](/images/cards/code.svg)
  </Card>
</Cards>